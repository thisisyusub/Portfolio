name: portfolio
description: portfolio of Kanan Yusubov

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^9.2.0
  google_fonts: ^2.2.0
  cupertino_icons: ^1.0.2
  url_strategy:
  url_launcher: ^6.0.18
  scroll_to_index: ^2.1.1
  provider: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - lib/assets/
